import Register from "../../components/Register";

export const metadata = {
  title: "Register - Auth Demo",
  description: "Create a new account",
};

export default function RegisterPage() {
  return <Register />;
}
